# Extracted pattern PNGs

2bpp art rendered from authentic TERSE `PATTERN` / `~` `^` sources (and GORF4A frames). Used by the pattern lab and as human-readable checks against `XC.PATTERNS` decode.

Regenerate / expand via the extract + export tools under `tools/` (see root README). Metadata: `from_floppy_files.json`, `gorf4a_meta.json`.

Browse in-repo: open [`../../play/index.html`](../../play/index.html) after `python3 tools/export_play_assets.py`.
