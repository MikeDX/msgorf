# Next phase plan

1. **Source fidelity** — pattern lab only in `play/`; no fake game
2. **Python TERSE parse/dict/compile** — grow `tools/terse/` against PATTERN + BULLETS + XCLOAD
3. **C runtime** — blit/collision → SDL/WASM harness for *compiled* output
4. **Hunt `XC.LOGIC`** / more floppies for application screens
5. **ROM image** — `XC.PATTERNS` @ 0x4000 + logic when real

Primary goal remains a **compiled ROM**.
