# Reconstruction policy

**Do not** put guessed gameplay into `play/`.

Guessed or host-only experiments go under `reconstructed/` with a `GUESS:` header and a lab-notebook entry.

Authentic sources stay in `extracted/` and originals under `MSGORF*` folders.
