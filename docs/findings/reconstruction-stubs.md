# Reconstruction policy

**Do not** put guessed gameplay into `play/`.

Guessed or host-only experiments go under `reconstructed/` with a `GUESS:` header and a lab-notebook entry.

Authentic sources stay in `extracted/` and originals under `MSGORF*` folders.

## Video-derived remake (optional)

Prototype footage (Jamie Fenton 1982 interview) may inform a **labeled** playable sketch that reuses disk pattern art + human/AI motion estimates.

- Footage notes + shot list: [`../references/jamie-fenton-1982-video.md`](../references/jamie-fenton-1982-video.md)
- Frames / notes: prefer `reconstructed/video_frames/` or `docs/references/jamie_fenton_1982_video/`
- Code: `reconstructed/` only; never merge guessed AI into `play/` or the SDL pattern validator
- README / `GUESS:` must say this is **not** compiled Ms. Gorf / not `XC.LOGIC`
