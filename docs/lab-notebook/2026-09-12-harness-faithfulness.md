# Lab notebook — 2026-09-12 — Faithfulness: harness → validator

## Trigger

User: `make run` is fine only if faithful; pointed at https://www.professionalmagic.com/gorf.

## Changes

- Removed invented Gorf drift / fake “shmup” motion from `sdl_main.c`.
- Default `make run` = **static atlas** of XC-decoded (or source-pack) patterns.
- `--probe` / `make probe` = optional overlay-collision ABI with labelled lab keyboard (Jamie write-cycle model); not game controls.
- Lab greyscale palette; frame size still a documented guess.
- Captured page notes in `docs/references/professionalmagic-gorf.md` (TERSE 78, separate pattern/logic disks, dual-stick prototype, write-cycle collision quote, PDF links).

## Still missing for faithful play

`XC.LOGIC` / application TERSE, dual-stick map, cabinet resolution/colors, speech.
