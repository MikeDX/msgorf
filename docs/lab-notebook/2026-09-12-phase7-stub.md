# 2026-09-12 — Phase 7 ROM stub

`tools/assemble_rom_stub.py` exits 2 while completeness gate is PARTIAL and writes `out/assemble_report.json`.

Deliverables under `out/` document the blocker and package XC manifest + MVP object only.
